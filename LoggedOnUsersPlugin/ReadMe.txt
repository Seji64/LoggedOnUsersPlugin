LoggedOn Users Plugin
v1.0.2

Plugin to suppress standby when one or more defined users are logged in and active

Plugin-Einstellungen:
- Displays a list of available users. If at least one of the selected users is active, the standby mode is suppressed.